version: 2
renderer: networkd

ethernets:
  lan:
  # enp0s3:
    match:
      macaddress: "${VM_MAC}"
    set-name: enp0s3
    optional: true
    dhcp4: true
    dhcp6: false
    dhcp-identifier: mac
